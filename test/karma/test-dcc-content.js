"use strict";

describe( "#DirectCurrencyContent", function() {

    it("should update settings", function() {
        const settings = {};
        DirectCurrencyContent.onUpdateSettings(settings);
    });

});